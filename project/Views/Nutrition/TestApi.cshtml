@{
    ViewData["Title"] = "API Test";
    Layout = "_Layout";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">🔧 Gemini API Test</h3>
                </div>
                <div class="card-body">
                    @if (ViewBag.Success == true)
                    {
                        <div class="alert alert-success">
                            <h5>@ViewBag.Message</h5>
                            <hr>
                            <strong>API Yanıtı:</strong>
                            <div class="mt-3 p-3 bg-light rounded" style="white-space: pre-wrap; font-family: monospace;">
                                @ViewBag.Response
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-danger">
                            <h5>@ViewBag.Message</h5>
                            <hr>
                            <strong>Hata Detayı:</strong>
                            <div class="mt-3 p-3 bg-light rounded" style="white-space: pre-wrap; font-family: monospace; color: #dc3545;">
                                @ViewBag.Error
                            </div>
                        </div>
                    }
                    
                    <div class="mt-4">
                        <a href="@Url.Action("Create", "Nutrition")" class="btn btn-primary">
                            <i class="bi bi-arrow-left"></i> Beslenme Programı Oluştur
                        </a>
                        <a href="@Url.Action("TestApi", "Nutrition")" class="btn btn-secondary">
                            <i class="bi bi-arrow-clockwise"></i> Tekrar Test Et
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 12px;
    }
    .card-header {
        border-radius: 12px 12px 0 0 !important;
    }
</style>

